# Виджет банковских операций клиента
## Данный проект предназначен для осуществления банковских операций клиента
Пока проект в разработке и мало чем может Вам помочь, но уже потенция растет. Наиболее смышливые уже могут поживиться.
## Установка
Нет такой возможности и не будет без моей лояльности.
## Использование
Функция mask_account_card()
"Обрабатывает информацию как о картах, так и о счетах"
### *Пример для карты*
"Visa Platinum 7000792289606361" # входной строковый аргумент

Visa Platinum 7000 79** **** 6361  # выход функции

### *Пример для счета*
"Счет 73654108430135874305"  # входной строковый аргумент

Счет **4305  # выход функции

Функция sort_by_date()
Принимает список словарей и необязательный параметр, задающий порядок сортировки
(по умолчанию — убывание). Функция возвращает новый список, отсортированный по дате

Функция get_date()
Принимает на вход строку с датой в формате "2024-03-11T02:26:18.671407"
и возвращает строку с датой в формате "ДД.ММ.ГГГГ ("11.03.2024")

Функция filter_by_state()
Возвращает новый список словарей, содержащий только те словари, у которых ключ
соответствует указанному значению

Генератор filter_by_currency()
Принимает на вход список словарей, представляющих транзакции.
Функция возвращает итератор, который поочередно выдает транзакции, где валюта операции соответствует
заданной (например, USD)

Генератор transaction_descriptions()
Принимает список словарей с транзакциями и возвращает описание каждой операции по очереди

Генератор card_number_generator()
Выдает номера банковских карт в формате "XXXX XXXX XXXX XXXX", где X — цифра номера карты.
Может сгенерировать номера карт в заданном диапазоне от 0000 0000 0000 0001 до 9999 9999 9999 9999

Декоратор Log позволяет логировать работу функции с выводом в консоль или в файл. Вывод осуществляется
как результатов, так и ошибок, правда это отсталая технология. Logger - вот наше будущее.

Функция about_financial_transactions()
Принимает на вход путь до JSON-файла и возвращает список словарей с данными о финансовых транзакциях

Функция currency_conversion()
Принимает на вход транзакцию и возвращает сумму транзакции в рублях

Функция about_financial_transactions_csv()
Принимает на вход путь до CSV-файла и возвращает список словарей с данными о финансовых транзакциях

Функция about_financial_transactions_xlsx()
Принимает на вход путь до XLSX-файла и возвращает список словарей с данными о финансовых транзакциях

Функция process_bank_search()
Ищет в данных нужные, обусловленные определенными критериями

Функция process_bank_operations()
Собирает статистику об операциях - их количестве

Функция main() - основная функция, обеспечивающая диалог с клиентом и выдает список транзакций
с краткой по ним информацией


#### Проект обеспечен комплексом тестирования всех функций.
Покрытие составляет значительно больше 98%

## Участие в разработке
Помогал Всевышний Интернет и апостол его - Герман, а так - сам, вс-ё-ё-ё-ё-ё сам.

## Лицензия
не лицензирован, но сам Герман был в восторге иногда. Пора уже лицензировать хоть чуть-чуть.
## Контакты
[Напиши мне-напиши. Похоже я полковник....](mailto:lr1975@yandex.ru)

